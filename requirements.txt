# Core Telegram Bot
python-telegram-bot==20.7

# HTTP Requests
requests==2.31.0

# Environment Variables
python-dotenv==1.0.0

# Google Gemini AI
google-generativeai==0.3.2

# Optional but recommended for better performance
# aiohttp==3.9.1  # For async HTTP requests (if you add more features)

# Note: No PyTorch - Render free tier has 512MB RAM limit
# Note: No OpenAI package - we're using direct API calls